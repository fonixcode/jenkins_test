agent {
    docker {
        image "fonix/fonix_pg_redis_ruby2.1"
    }

    stages {
      stage("test") {
        steps {
          sh "id"
          sh "pwd"
          sh "echo $HOME"
           
        }
      }
    }
}
